'use strict';const {authorized}=require('../lib/dashboard-auth');module.exports=async(req,res)=>{if(!authorized(req))return res.status(401).json({ok:false,error:'Dashboard authentication required'});return res.status(410).json({ok:false,error:'Legacy Google Sheets dashboard endpoint retired. Use /api/dashboard-live.'})};
