'use strict';
const {secret,authorized,setCookie}=require('../lib/dashboard-auth');
module.exports=async(req,res)=>{if(req.method!=='POST')return res.status(405).send('Method Not Allowed');const s=secret();if(!s)return res.status(503).send('Dashboard authentication is not configured');try{let body=req.body;if(typeof body==='string'){if((req.headers['content-type']||'').includes('application/x-www-form-urlencoded'))body=Object.fromEntries(new URLSearchParams(body));else body=JSON.parse(body||'{}')}const password=String(body?.password||'');if(password!==s)return res.status(401).send('<p style="font:16px system-ui;text-align:center;margin-top:20vh">Invalid dashboard password. <a href="/dashboard/">Try again</a></p>');setCookie(res);return res.redirect(303,'/dashboard/')}catch(e){console.error('[DASHBOARD AUTH]',e.message);return res.status(400).send('Invalid request')}};
