const fs=require('node:fs');const path=require('node:path');
const root=path.join(process.cwd(),'dashboard');
module.exports=(req,res)=>{try{const raw=String(req.url||'').split('?')[0].replace(/^\/dashboard\/?/,'');const file=raw||'index.html';const allowed=new Set(['index.html','dashboard.css','dashboard.js','enhancements.js']);if(!allowed.has(file))return res.status(404).send('Not Found');const ext=path.extname(file);const type=ext==='.html'?'text/html; charset=utf-8':ext==='.css'?'text/css; charset=utf-8':'application/javascript; charset=utf-8';let data=fs.readFileSync(path.join(root,file),'utf8');if(file==='index.html')data=data.replace('</body>','<script src="/dashboard/enhancements.js" defer></script></body>');res.setHeader('Content-Type',type);res.setHeader('Cache-Control','public, max-age=60, must-revalidate');return res.status(200).send(data)}catch(e){console.error('[DASHBOARD]',e.message);return res.status(500).send('Dashboard unavailable')}};